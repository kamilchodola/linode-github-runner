export declare type Capabilities = 'Linodes' | 'NodeBalancers' | 'Block Storage' | 'Object Storage' | 'Kubernetes' | 'GPU Linodes' | 'Cloud Firewall' | 'Vlans' | 'Bare Metal';
export interface DNSResolvers {
    ipv4: string;
    ipv6: string;
}
export declare type RegionStatus = 'ok' | 'outage';
export interface Region {
    id: string;
    country: string;
    capabilities: Capabilities[];
    status: RegionStatus;
    resolvers: DNSResolvers;
}
